<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"> <%= image_tag(image_path('logo-website.png'), width: '150px') %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <%= link_to "Locations", locations_path, class: 'nav-link px-2 link-success' %>
      <%= link_to "Categories", categories_path, class: 'nav-link px-2 link-success' %>
        </li>
        <%= link_to "Request Service", new_request_path, class: 'btn btn-outline-success' %>
        <% if user_signed_in? %>
        <%= link_to "My Requests", myrequests_path, class: 'btn btn-warning' %>
        <% end %>

        </ul>

       
        <% if user_signed_in? %>
        <div class="dropdown text-end">
          <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
          <% if current_user.avatar.attached? %>
          <%= image_tag current_user.avatar, class: 'rounded-circle', width: '30' %>  
          <% else %>
          <%= image_tag(image_path('profile.svg'), class: 'rounded-circle', width: '30') %>
          <% end %>
          <span><%= current_user.firstname %> <%= current_user.lastname %></span>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
             <%= link_to "My Requests", myrequests_path, class: 'dropdown-item' %>
             <li><hr class="dropdown-divider"></li>
             <li><%#= link_to "View Profile", user_path(@user), class: 'dropdown-item' %></li>
            <li><a class="dropdown-item" href="/"><%= current_user.roles.pluck(:name) %><br><%= current_user.firstname %> <%= current_user.lastname %></a></li>
            <li><%= link_to "Profile Settings", edit_user_registration_path, class: 'dropdown-item' %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to 'Sign out', destroy_user_session_path, :method => :delete, class: 'dropdown-item' %></li>
          </ul>
        </div>
        <% else %>
        <form class="d-flex">
        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        <%= link_to "Sign in", new_user_session_path, class: 'btn btn-outline-success' %>
        <%= link_to "Sign up", new_user_registration_path, class: 'btn btn-success' %>
        </ul>
        </form>
        <% end %>
    </div>
  </div>
</nav>



